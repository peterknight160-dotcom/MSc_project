[package]
name = "lattice_keyber_combined_test"
version = "0.1.0"
edition = "2024"

[dependencies]
lattice-kyber = { version = "0.1", features = ["getrandom"] }
useful_stats = { path = "../useful_stats" }
env = "1.0.1"